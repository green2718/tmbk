To generate the DB, run `db_generation.php`. You can use this script to update your DB from a previous revision.
To fill the DB, run `db_filling.php`. It checks if the picture isn't in the DB before adding it.
